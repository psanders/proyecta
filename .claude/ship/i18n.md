# Ship checkpoint — i18n

Started: 2026-09-15
Current stage: 3 — Build (common + API done; dashboard next, on top of feat/dark-mode)

**Scope:** The owner dashboard in Spanish (default) and English, mirroring QCobro's library-free i18n. The language is chosen in Mi perfil, saved per user in a Proyecta `UserSettings` row, cached in the browser, and the dashboard API returns validation, permission and domain errors in the requester's language (`x-language`). Player, web and `/device/v1` stay Spanish/unchanged.

**Detected surfaces:** OpenSpec: yes · Pencil: yes · Storybook: no · E2E: yes (Playwright)

**Worktree:** `.claude/worktrees/i18n`, branch `feat/i18n` from `feat/ppd-nav-favicon` @ 95fc954 (origin/main lacks the account menu / settings work).

| # | Stage | Status | Notes |
| :- | :--- | :--- | :--- |
| 0 | Frame | done | Change created with /opsx:propose; `openspec validate i18n --strict` passes |
| 1 | Design (Pencil) | done | Approved 2026-09-15. `profile` frame (z7nmZp) in the main checkout's pencil.pen gained an Idioma card (select "Español") between Contraseña and Apariencia; frame 1160 tall |
| 2 | Spec reconcile | done | Design matched the spec. Build added one scenario: the delete confirmation accepts DELETE (English) as well as ELIMINAR |
| 3 | Build | in-progress | Common (catalog, language schema, ValidationError ids) and API (UserSettings, validated functions, x-language, localized errors, profile.updateLanguage) done: 29 common + 75 API unit tests. Dashboard pending |
| 4 | Test | pending | |
| 5 | Sync | pending | Human gate |
| 6 | Archive | pending | Human gate |

Status values: `pending` · `in-progress` · `done` · `skipped` (with reason).

## Decision log

Newest first. One line per meaningful decision or stage transition.

- 2026-09-15 — Dark mode (feat/dark-mode, PR open) rewrites ProfilePage, AppSidebar and strings.ts, so feat/i18n is rebased onto feat/dark-mode before the dashboard migration. The i18n PR must merge after dark mode.
- 2026-09-15 — Delete-business confirmation accepts ELIMINAR or DELETE, so English users type an English word (spec scenario added).
- 2026-09-15 — Device protocol schemas keep literal English messages (never ids), so /device/v1 responses are unchanged. The dashboard's pairing-code field error stays English as before.
- 2026-09-15 — Design approved; spec reconcile found no changes.

- 2026-09-15 — Idioma is its own card in the `profile` frame (z7nmZp), not inside Apariencia: language is saved to the account and applies instantly, while Apariencia is per browser. Subtitle now mentions the language. A duplicate `my-profile` frame created before `profile` appeared was deleted. Edited in the main checkout's design/pencil.pen per product owner.

- 2026-09-15 — API messages: schemas/DomainError/TRPCError use message ids from a common es/en catalog; ValidationError resolves to Spanish (logs/tests unchanged); tRPC errorFormatter re-resolves per `x-language`. QCobro has no equivalent (its schemas carry no custom text).
- 2026-09-15 — Before sign in: browser cache, else English if navigator language is en*, else Spanish.
- 2026-09-15 — Implementation mirrors ../qcobro/mods/webapp (lib/i18n.tsx flat message ids, useI18n, languageNames endonyms, localStorage cache, usePreferenceSync, UserSettings model, createUpdateUserLanguage). Product owner's call.
- 2026-09-15 — Scope: dashboard only (+ the API messages it shows). Language stored per user on the server. Product owner's call.
- 2026-09-15 — Work in a separate worktree (product owner's request).
- 2026-09-15 — Checkpoint created; framing the change.
