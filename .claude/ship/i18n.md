# Ship checkpoint — i18n

Started: 2026-09-15
Current stage: done

**Scope:** The owner dashboard in Spanish (default) and English, mirroring QCobro's library-free i18n. The language is chosen in Mi perfil (one Preferencias card with Idioma and Apariencia rows), saved per user in a Proyecta `UserSettings` row once chosen, cached in the browser, and the dashboard API returns validation, permission and domain errors in the requester's language (`x-language`). Player, web and `/device/v1` stay Spanish/unchanged.

**Detected surfaces:** OpenSpec: yes · Pencil: yes · Storybook: no · E2E: yes (Playwright)

**Worktree:** `.claude/worktrees/i18n`, branch `feat/i18n`, rebased onto origin/main after dark mode (PR #11) merged. Pencil edits live in the MAIN checkout's design/pencil.pen.

| # | Stage | Status | Notes |
| :- | :--- | :--- | :--- |
| 0 | Frame | done | Change created with /opsx:propose; `openspec validate i18n --strict` passes |
| 1 | Design (Pencil) | done | Approved 2026-09-15, revised the same day: `profile` frame (z7nmZp) in the main checkout's pencil.pen has one Preferencias card with Idioma and Apariencia setting rows; frame 1080 tall |
| 2 | Spec reconcile | done | Design matched the spec. Build added: delete confirmation accepts DELETE; a user who never chose a language keeps the browser's language (profile language is null until chosen) |
| 3 | Build | done | Common catalog + language schema; API UserSettings, validated functions, x-language, localized errors, profile.updateLanguage; dashboard lib/i18n.ts store, messages/{es,en}.ts, useI18n, usePreferenceSync, SettingRow, LanguageSelect; strings.ts and common `*_LABELS` removed; CLAUDE.md + config.yaml rule updated |
| 4 | Test | done | lint + typecheck green; unit common 29, api 78, dashboard 22, player 9, web 5; integration 17; e2e 16/16 incl. new English test. English screenshot pass: two copy fixes |
| 5 | Sync | done | Approved 2026-09-15 after PR #13 merged: new `openspec/specs/dashboard-language`, MODIFIED "Theme preference" in `dashboard-appearance` |
| 6 | Archive | done | `openspec/changes/archive/2026-09-15-i18n` |

Status values: `pending` · `in-progress` · `done` · `skipped` (with reason).

## Decision log

Newest first. One line per meaningful decision or stage transition.

- 2026-09-15 — PR #13 merged; specs synced and change archived on chore/sync-archive-i18n (product owner approved).

- 2026-09-15 — Test stage green. Stopped nothing: the dark-mode worktree's dev servers on :3000/:5175 had already exited when approved to stop them (Playwright reuses whatever is on those ports).
- 2026-09-15 — Found in the English screenshot pass: a new user signing up from an English browser was switched to Spanish (profile defaulted to es). Now `profile.get` returns `language: null` until chosen and the dashboard keeps the browser language; spec scenario "Sign up from an English browser" added.
- 2026-09-15 — Fixed a race in SettingsPage (field handlers spread a stale `form`, wiping the name when the time zone changed right as settings loaded); made the "time zone" e2e flaky 1/3. ScreenFormPage has the same stale-spread pattern; left as is (out of scope, not seen failing).
- 2026-09-15 — Playwright `use.locale: "es-DO"`: the dashboard now picks English for English browsers on first visit, and the existing suites assert Spanish.
- 2026-09-15 — Language store instead of QCobro's React context: `lib/i18n.ts` mirrors dark mode's injectable `lib/theme.ts` (testable without DOM, readable by the tRPC link). Public API matches QCobro (`useI18n` → language, setLanguage, t; flat ids; endonyms).
- 2026-09-15 — Product owner: no card per option. Pencil `profile` now has one "Preferencias" card of setting rows (Idioma, Apariencia) instead of separate Idioma/Apariencia cards. Title "Preferencias" recommended over "Otros" (says what's inside; alternatives: Personalización, Ajustes del panel). Row hints keep the scopes clear (account vs this browser).
- 2026-09-15 — Dark mode merged to main (PR #11); feat/i18n rebased onto origin/main.
- 2026-09-15 — Delete-business confirmation accepts ELIMINAR or DELETE, so English users type an English word (spec scenario added).
- 2026-09-15 — Device protocol schemas keep literal English messages (never ids), so /device/v1 responses are unchanged. The dashboard's pairing-code field error stays English as before.
- 2026-09-15 — API messages: schemas/DomainError/TRPCError use message ids from a common es/en catalog; ValidationError resolves to Spanish (logs/tests unchanged); tRPC errorFormatter re-resolves per `x-language`. QCobro has no equivalent (its schemas carry no custom text).
- 2026-09-15 — Before sign in: browser cache, else English if navigator language is en*, else Spanish.
- 2026-09-15 — Implementation mirrors ../qcobro/mods/webapp (flat message ids, useI18n, languageNames endonyms, localStorage cache, usePreferenceSync, UserSettings model, createUpdateUserLanguage). Product owner's call.
- 2026-09-15 — Scope: dashboard only (+ the API messages it shows). Language stored per user on the server. Product owner's call.
- 2026-09-15 — Work in a separate worktree (product owner's request).
- 2026-09-15 — Checkpoint created; framing the change.
