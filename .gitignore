# Temporary context, deleted manually later. Contains confidential material: never commit.
cleanup/

node_modules/
dist/
**/src/generated/
*.tsbuildinfo
.env
.env.*
!.env.example
playwright-report/
test-results/
.DS_Store
.claude/settings.local.json
.claude/ship/*.local.md

# Generated demo media and local data
.data/

# Local Identity secrets (generated by scripts/setup-identity.sh)
config/identity/identity.json
config/proyecta.json
config/identity/keys/
# Local ad creatives at the repo root only (not source folders named "ads").
/ads/

# Production TLS certs (copied onto the droplet by scripts/deploy/tls.sh) and
# database backups (scripts/deploy/backup-db.sh) — never part of the repo.
config/certs/
backups/
