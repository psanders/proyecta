org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Where the bundled player sends /device/v1 and fetches media. Override for local runs, e.g.
# ./gradlew installDebug -PproyectaApiBase=http://10.0.2.2:3000 (the emulator's view of localhost).
proyectaApiBase=https://api.proyecta.do
